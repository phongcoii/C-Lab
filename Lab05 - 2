#include <stdio.h>

void main()
{
    int a = 0, b = 0;
    int s;
    
    printf("Nhap so : ");
    scanf("%d", &s);
    
    for ( a = 0; s >= 10; a += b)
    {
        b= s%10;
        s= (s-b)/10;
    }
        printf("%d\n", a+s);
        
    
}

